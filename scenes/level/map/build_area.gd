extends TileMapLayer


func _on_hud_on_building_factory() -> void:
	pass
	#self.visible = true
	
	#for tile in get_used_cells():
		#var doit = int(randf_range(0, 2))
		#if doit == 1:
			#pass
			##set_cell(tile, 0, get_cell_atlas_coords(tile), 2)
		#else:
			#set_cell(tile, 0, get_cell_atlas_coords(tile), 3)
